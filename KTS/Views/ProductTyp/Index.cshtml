@model IEnumerable<KTS.Models.ProductType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.productSupplier)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SupplierContactDetails)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.productSupplier)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SupplierContactDetails)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.productTy }) |
            @Html.ActionLink("Details", "Details", new { id=item.productTy }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.productTy })
        </td>
    </tr>
}

</table>
